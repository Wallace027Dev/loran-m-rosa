import styled from 'styled-components';

export const Form = styled.form`
  button {
    margin-top: 4.5rem;
    width: 100%;
  }

  input {
    margin-bottom: 0.8rem;
  }

  .select-type {
    margin-top: 1.6rem;
  }
`;
