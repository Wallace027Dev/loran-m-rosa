import styled from 'styled-components'

export const Container = styled.main`
  .btnSign {
    color: var(--font-light);
  }

  .slogan {
    h2 {
      max-width: 60vw;
      top: 30%;
      color: var(--primary);
      font-weight: bold;
    }
  }
`
